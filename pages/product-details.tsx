// Use Material UI
import { Box } from "@mui/system";

type TProduct = {
  description: string;
  category: string;
  image: string;
  title: string;
  price: number;
};

export default function ProductDetails() {
  return <Box>Start Here</Box>;
}
